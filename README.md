# Go Tools

Install Go language and gopls (Language Server).

## Installation

Install via Xed-Editor's extension marketplace or from a ZIP file (Settings > Extensions > Install from storage).

Verify:
```bash
gopls --help
```

## Build

```bash
./compileDebug
# or release
./compileRelease
```